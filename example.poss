$color = blue

struct makeRound ($rad = 12px) 
	border-radius = $rad
	-webkit-border-radius = $rad

id header 
	makeRound

class footer 
	@inherit<id>(header)
	makeRound = 10px

id content 
	background-color = $color

id test-inherit
	@inherit<class>(footer)
	font-size = 12px

